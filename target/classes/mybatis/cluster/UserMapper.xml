<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mytian.data.link.mapper.cluster.UserMapper">

    <resultMap type="com.mytian.data.link.entity.User" id="baseUserResultMap">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
    </resultMap>

    <!--<insert id="saveLink" parameterType="com.mytian.data.link.entity.Link">
        insert into link(id,link)
        values(#{id},#{link})
    </insert>-->
    <!--<select id="findUser" parameterType="com.mytian.data.link.entity.User">
       select user from user where username = #{username} and password = #{password}

    </select>-->

</mapper>